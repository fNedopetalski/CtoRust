#include <stdio.h>

int func(int v, int c) {
    return v*c;
}

struct {
    float bb;
    int aa;
    int cc;
} exemplo;

int main(int argc) {
    int a = 10;
    int b = 30;
    int c = (a + b) / b;
    c = c / (a * b);
    if (c > 0) {
        while (c <= 20) {
            c = c+1; 
        }
    }
}
