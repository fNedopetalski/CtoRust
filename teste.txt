#include <stdio.h>

int a = 10;
int b;

int func(int v, int c) {
    return v*c;
}

struct {
    float b;
    int a;
    int c;
} exemplo;

int main(int argc) {
    b = 30;
    int c = (a + b) / b;
    c = c / (a * b);
    if (c > 0) {
        while (c > 0) {
            c = c+1;
        }
    }
}
